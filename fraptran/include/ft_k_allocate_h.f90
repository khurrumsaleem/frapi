if(.not. allocated(cexh2a               ) ) allocate( cexh2a                      (1:naxialnodes) )
if(.not. allocated(deltox               ) ) allocate( deltox                      (1:naxialnodes) )
if(.not. allocated(explenumt            ) ) allocate( explenumt                      (1:2*ntimesteps+2) )
if(.not. allocated(tEnd                 ) ) allocate( tEnd                      (1:ntimepairs) )
if(.not. allocated(gfInt                ) ) allocate( gfInt                      (1:ntimepairs) )
if(.not. allocated(edInt                ) ) allocate( edInt                      (1:ntimepairs) )
if(.not. allocated(CladdingPower        ) ) allocate( CladdingPower                      (1:naxialnodes) )
if(.not. allocated(RadPowProfile        ) ) allocate( RadPowProfile                      (1:2*nradialnodes*naxialnodes) )
if(.not. allocated(radtemp              ) ) allocate( radtemp                      (1:naxialnodes,1:ntimesteps) )
if(.not. allocated(fuelrad              ) ) allocate( fuelrad                      (1:naxialnodes,1:ntimesteps) )
if(.not. allocated(ndchfi               ) ) allocate( ndchfi                      (1:naxialnodes) )
if(.not. allocated(bowrat               ) ) allocate( bowrat                      (1:naxialnodes) )
if(.not. allocated(techfi               ) ) allocate( techfi                      (1:naxialnodes) )
if(.not. allocated(tschfi               ) ) allocate( tschfi                      (1:naxialnodes) )
if(.not. allocated(hlqcl1               ) ) allocate( hlqcl1                      (1:ntimesteps) )
if(.not. allocated(fdial                ) ) allocate( fdial                      (1:30) )
if(.not. allocated(ibnopt               ) ) allocate( ibnopt                      (1:33) )
if(.not. allocated(nopt                 ) ) allocate( nopt                      (1:8) )
if(.not. allocated(nunopt               ) ) allocate( nunopt                      (1:14) )
if(.not. allocated(idumr1               ) ) allocate( idumr1                      (1:7) )
if(.not. allocated(vplen                ) ) allocate( vplen                      (1) )
if(.not. allocated(apln0                ) ) allocate( apln0                      (1) )
if(.not. allocated(bpln0                ) ) allocate( bpln0                      (1) )
if(.not. allocated(vsn                  ) ) allocate( vsn                      (1) )
if(.not. allocated(bu                   ) ) allocate( bu                      (1) )
if(.not. allocated(gappr0               ) ) allocate( gappr0                      (1) )
if(.not. allocated(scd                  ) ) allocate( scd                      (1) )
if(.not. allocated(spl                  ) ) allocate( spl                      (1) )
if(.not. allocated(swd                  ) ) allocate( swd                      (1) )
if(.not. allocated(qpln                 ) ) allocate( qpln                      (1) )
if(.not. allocated(qpln0                ) ) allocate( qpln0                      (1) )
if(.not. allocated(flwblk               ) ) allocate( flwblk                      (1) )
if(.not. allocated(tp                   ) ) allocate( tp                      (1) )
if(.not. allocated(powave               ) ) allocate( powave                      (1) )
if(.not. allocated(dvdt                 ) ) allocate( dvdt                      (1) )
if(.not. allocated(apln                 ) ) allocate( apln                      (1) )
if(.not. allocated(bpln                 ) ) allocate( bpln                      (1) )
if(.not. allocated(dvdt0                ) ) allocate( dvdt0                      (1) )
if(.not. allocated(hfusn                ) ) allocate( hfusn                      (1:3) )
if(.not. allocated(tmelt                ) ) allocate( tmelt                      (1:3) )
if(.not. allocated(ascal3               ) ) allocate( ascal3                      (1:25) )
if(.not. allocated(ascal2               ) ) allocate( ascal2                      (1:57) )
if(.not. allocated(ascal1               ) ) allocate( ascal1                      (1:29) )
if(.not. allocated(gadolin              ) ) allocate( gadolin                      (1:naxialnodes) )
if(.not. allocated(pchn                 ) ) allocate( pchn                      (1,1) )
if(.not. allocated(burad                ) ) allocate( burad                      (1:naxialnodes,1:nradialnodes) )
if(.not. allocated(radpow               ) ) allocate( radpow                      (1:naxialnodes,1:nradialnodes) )
if(.not. allocated(radsrc               ) ) allocate( radsrc                      (1:naxialnodes,1:nradialnodes) )
if(.not. allocated(radsrco              ) ) allocate( radsrco                      (1:naxialnodes,1:nradialnodes) )
if(.not. allocated(butemp               ) ) allocate( butemp                      (1:naxialnodes*nradialnodes) )
if(.not. allocated(aprntb               ) ) allocate( aprntb                      (1) )
if(.not. allocated(ies                  ) ) allocate( ies                      (1) )
if(.not. allocated(pswll0               ) ) allocate( pswll0                      (1) )
if(.not. allocated(roi                  ) ) allocate( roi                      (1) )
if(.not. allocated(vs0                  ) ) allocate( vs0                      (1) )
if(.not. allocated(flowg                ) ) allocate( flowg                      (1) )
if(.not. allocated(GasAx                ) ) allocate( GasAx                      (1:naxialnodes) )
if(.not. allocated(ProfileStartTime     ) ) allocate( ProfileStartTime                      (1:ntimesteps) )
if(.not. allocated(RodAvePower          ) ) allocate( RodAvePower                      (1:2*ntimesteps) )
if(.not. allocated(AxPowProfile         ) ) allocate( AxPowProfile                      (1:2*naxialnodes,1:ntimesteps) )
if(.not. allocated(pazp                 ) ) allocate( pazp                      (1:2*nradialnodes, 1:naxialnodes) )
if(.not. allocated(cmesh                ) ) allocate( cmesh                      (1:nradialnodes) )
if(.not. allocated(fmesh                ) ) allocate( fmesh                      (1:nradialnodes) )
if(.not. allocated(zelev                ) ) allocate( zelev                      (1:naxialnodes) )
if(.not. allocated(BoundaryCondition    ) ) allocate( BoundaryCondition                      (1:6) )
if(.not. allocated(ThermalConductAdv    ) ) allocate( ThermalConductAdv                      (1:nradialnodes) )
if(.not. allocated(FinalTemp            ) ) allocate( FinalTemp                      (1:nradialnodes) )
if(.not. allocated(PrevTemp             ) ) allocate( PrevTemp                      (1:nradialnodes) )
if(.not. allocated(ArrayE               ) ) allocate( ArrayE                      (1:nradialnodes) )
if(.not. allocated(ArrayF               ) ) allocate( ArrayF                      (1:nradialnodes) )
if(.not. allocated(VolumeWeightL        ) ) allocate( VolumeWeightL                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(VolumeWeightR        ) ) allocate( VolumeWeightR                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(AreaWeight           ) ) allocate( AreaWeight                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(ThermalConductivity  ) ) allocate( ThermalConductivity                      (1:nradialnodes) )
if(.not. allocated(acond                ) ) allocate( acond                      (1:30,1:10) )
if(.not. allocated(RhoCp                ) ) allocate( RhoCp                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(RhoCp0               ) ) allocate( RhoCp0                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(PrevIterateTemp      ) ) allocate( PrevIterateTemp                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(relfraca             ) ) allocate( relfraca                      (1:2*ntimesteps) )
if(.not. allocated(FuelGasSwell         ) ) allocate( FuelGasSwell                      (1:2*ntimesteps) )
if(.not. allocated(ansr                 ) ) allocate( ansr                      (1:nradialnodes) )
if(.not. allocated(fmgp                 ) ) allocate( fmgp                      (1:naxialnodes) )
if(.not. allocated(gasavail1            ) ) allocate( gasavail1                      (1:naxialnodes,1:nradialnodes)) ! 1:ngas )
if(.not. allocated(gasavail2            ) ) allocate( gasavail2                      (1:naxialnodes,1:nradialnodes)) ! 1:ngas )
if(.not. allocated(farbal               ) ) allocate( farbal                      (1:20) )
if(.not. allocated(sdfar                ) ) allocate( sdfar                      (1:20) )
if(.not. allocated(zfarbl               ) ) allocate( zfarbl                      (1:20) )
if(.not. allocated(ablona               ) ) allocate( ablona                      (1:73) )
if(.not. allocated(IndexTempConverg     ) ) allocate( IndexTempConverg                      (1:naxialnodes) )
if(.not. allocated(RadialPower          ) ) allocate( RadialPower                      (1:nradialnodes) )
if(.not. allocated(RadialBound          ) ) allocate( RadialBound                      (1:nradialnodes) )
if(.not. allocated(qmaxmelt             ) ) allocate( qmaxmelt                      (1:nradialnodes) )
if(.not. allocated(qmaxmeltp1           ) ) allocate( qmaxmeltp1                      (1:nradialnodes) )
if(.not. allocated(rmassflux            ) ) allocate( rmassflux                      (1:naxialnodes) )
if(.not. allocated(coolqual             ) ) allocate( coolqual                      (1:naxialnodes) )
if(.not. allocated(AAHT1                ) ) allocate( AAHT1                      (1:naxialnodes) )
if(.not. allocated(BBHT1                ) ) allocate( BBHT1                      (1:naxialnodes) )
if(.not. allocated(GasPress0            ) ) allocate( GasPress0                      (1:naxialnodes) )
if(.not. allocated(CoolPress            ) ) allocate( CoolPress                      (1:naxialnodes) )
if(.not. allocated(GasMolesAx           ) ) allocate( GasMolesAx                      (1:naxialnodes) )
if(.not. allocated(GasMolesAx0          ) ) allocate( GasMolesAx0                      (1:naxialnodes) )
if(.not. allocated(GasPress             ) ) allocate( GasPress                      (1:naxialnodes) )
if(.not. allocated(FuelSurfT            ) ) allocate( FuelSurfT                      (1:naxialnodes) )
if(.not. allocated(FuelCenterT          ) ) allocate( FuelCenterT                      (1:naxialnodes) )
if(.not. allocated(CrackTemp            ) ) allocate( CrackTemp                      (1:naxialnodes) )
if(.not. allocated(VolOpenPor           ) ) allocate( VolOpenPor                      (1:naxialnodes) )
if(.not. allocated(GapThick             ) ) allocate( GapThick                      (1:naxialnodes) )
if(.not. allocated(GapThick0            ) ) allocate( GapThick0                      (1:naxialnodes) )
if(.not. allocated(GapTemp              ) ) allocate( GapTemp                      (1:naxialnodes) )
if(.not. allocated(PelletRad            ) ) allocate( PelletRad                      (1:naxialnodes) )
if(.not. allocated(PelletRad0           ) ) allocate( PelletRad0                      (1:naxialnodes) )
if(.not. allocated(HeatFlux             ) ) allocate( HeatFlux                      (1:naxialnodes) )
if(.not. allocated(HeatFlux0            ) ) allocate( HeatFlux0                      (1:naxialnodes) )
if(.not. allocated(SurfHtFlux           ) ) allocate( SurfHtFlux                      (1:naxialnodes) )
if(.not. allocated(CladMaxT             ) ) allocate( CladMaxT                      (1:naxialnodes) )
if(.not. allocated(Vreloc               ) ) allocate( Vreloc                      (1:naxialnodes) )
if(.not. allocated(Vreloc0              ) ) allocate( Vreloc0                      (1:naxialnodes) )
if(.not. allocated(PelSrfDispl          ) ) allocate( PelSrfDispl                      (1:naxialnodes) )
if(.not. allocated(OldPelDis            ) ) allocate( OldPelDis                      (1:naxialnodes) )
if(.not. allocated(OldPelDis0           ) ) allocate( OldPelDis0                      (1:naxialnodes) )
if(.not. allocated(OxiThk1              ) ) allocate( OxiThk1                      (1:naxialnodes) )
if(.not. allocated(OxiThk2              ) ) allocate( OxiThk2                      (1:naxialnodes) )
if(.not. allocated(DishVperL            ) ) allocate( DishVperL                      (1:naxialnodes) )
if(.not. allocated(FastFlux             ) ) allocate( FastFlux                      (1:naxialnodes) )
if(.not. allocated(AlphaThk1            ) ) allocate( AlphaThk1                      (1:naxialnodes) )
if(.not. allocated(AlphaThk2            ) ) allocate( AlphaThk2                      (1:naxialnodes) )
if(.not. allocated(AlphaThk11           ) ) allocate( AlphaThk11                      (1:naxialnodes) )
if(.not. allocated(AlphaThk22           ) ) allocate( AlphaThk22                      (1:naxialnodes) )
if(.not. allocated(oxideid              ) ) allocate( oxideid                      (1:naxialnodes) )
if(.not. allocated(EffStrain            ) ) allocate( EffStrain                      (1:naxialnodes) )
if(.not. allocated(EffStrain0           ) ) allocate( EffStrain0                      (1:naxialnodes) )
if(.not. allocated(oxideod              ) ) allocate( oxideod                      (1:naxialnodes) )
if(.not. allocated(CldPermStrn          ) ) allocate( CldPermStrn                      (1:naxialnodes) )
if(.not. allocated(CldPermStrn0         ) ) allocate( CldPermStrn0                      (1:naxialnodes) )
if(.not. allocated(SSHgap               ) ) allocate( SSHgap                      (1:naxialnodes) )
if(.not. allocated(OldCoolPrs           ) ) allocate( OldCoolPrs                      (1:naxialnodes) )
if(.not. allocated(OldCoolPrs0          ) ) allocate( OldCoolPrs0                      (1:naxialnodes) )
if(.not. allocated(CladAveTemp          ) ) allocate( CladAveTemp                      (1:naxialnodes) )
if(.not. allocated(OldGasPrs            ) ) allocate( OldGasPrs                      (1:naxialnodes) )
if(.not. allocated(OldGasPrs0           ) ) allocate( OldGasPrs0                      (1:naxialnodes) )
if(.not. allocated(HtFlxFac             ) ) allocate( HtFlxFac                      (1:naxialnodes) )
if(.not. allocated(OldCladT             ) ) allocate( OldCladT                      (1:naxialnodes) )
if(.not. allocated(OldFuelAxStrn        ) ) allocate( OldFuelAxStrn                      (1:naxialnodes) )
if(.not. allocated(RodOD                ) ) allocate( RodOD                      (1:naxialnodes) )
if(.not. allocated(OldCldAxStrn         ) ) allocate( OldCldAxStrn                      (1:naxialnodes) )
if(.not. allocated(OldCldAxStrn0        ) ) allocate( OldCldAxStrn0                      (1:naxialnodes) )
if(.not. allocated(RodOD0               ) ) allocate( RodOD0                      (1:naxialnodes) )
if(.not. allocated(OldCladT0            ) ) allocate( OldCladT0                      (1:naxialnodes) )
if(.not. allocated(OldFuelAxStrn0       ) ) allocate( OldFuelAxStrn0                      (1:naxialnodes) )
if(.not. allocated(TimeofGBSep          ) ) allocate( TimeofGBSep                      (1:naxialnodes) )
if(.not. allocated(SwellDispl           ) ) allocate( SwellDispl                      (1:naxialnodes) )
if(.not. allocated(HmaxInitial          ) ) allocate( HmaxInitial                      (1:naxialnodes) )
if(.not. allocated(CoolEnthalpy         ) ) allocate( CoolEnthalpy                      (1:naxialnodes) )
if(.not. allocated(RInterfacPrs         ) ) allocate( RInterfacPrs                      (1:naxialnodes) )
if(.not. allocated(RInterfacPrs0        ) ) allocate( RInterfacPrs0                      (1:naxialnodes) )
if(.not. allocated(HGapAv               ) ) allocate( HGapAv                      (1:naxialnodes) )
if(.not. allocated(FilmCoeffAv          ) ) allocate( FilmCoeffAv                      (1:naxialnodes) )
if(.not. allocated(AxialPowr            ) ) allocate( AxialPowr                      (1:naxialnodes) )
if(.not. allocated(AxialPowr0           ) ) allocate( AxialPowr0                      (1:naxialnodes) )
if(.not. allocated(CritHtFlux           ) ) allocate( CritHtFlux                      (1:naxialnodes) )
if(.not. allocated(FuelSrfStrRat        ) ) allocate( FuelSrfStrRat                      (1:naxialnodes) )
if(.not. allocated(WatrMetlEnrgy        ) ) allocate( WatrMetlEnrgy                      (1:naxialnodes) )
if(.not. allocated(RInterfacGap         ) ) allocate( RInterfacGap                      (1:naxialnodes) )
if(.not. allocated(CladSurfT            ) ) allocate( CladSurfT                      (1:naxialnodes) )
if(.not. allocated(EDotFZ               ) ) allocate( EDotFZ                      (1:naxialnodes) )
if(.not. allocated(PelSrfStrn0          ) ) allocate( PelSrfStrn0                      (1:naxialnodes) )
if(.not. allocated(FuelSrfStrRat0       ) ) allocate( FuelSrfStrRat0                      (1:naxialnodes) )
if(.not. allocated(EDotFZ0              ) ) allocate( EDotFZ0                      (1:naxialnodes) )
if(.not. allocated(EnergyPerL           ) ) allocate( EnergyPerL                      (1:naxialnodes) )
if(.not. allocated(HFXSum               ) ) allocate( HFXSum                      (1:naxialnodes) )
if(.not. allocated(CladEnrgPerL         ) ) allocate( CladEnrgPerL                      (1:naxialnodes) )
if(.not. allocated(CoolEnthalpy0        ) ) allocate( CoolEnthalpy0                      (1:naxialnodes) )
if(.not. allocated(CoolMassFlx          ) ) allocate( CoolMassFlx                      (1:naxialnodes) )
if(.not. allocated(Enthl                ) ) allocate( Enthl                      (1:naxialnodes) )
if(.not. allocated(CoolDensity          ) ) allocate( CoolDensity                      (1:naxialnodes) )
if(.not. allocated(CoolDensity0         ) ) allocate( CoolDensity0                      (1:naxialnodes) )
if(.not. allocated(PelRadDeviat         ) ) allocate( PelRadDeviat                      (1:naxialnodes) )
if(.not. allocated(CldPermAxStrn        ) ) allocate( CldPermAxStrn                      (1:naxialnodes) )
if(.not. allocated(VoidVolumeRatio      ) ) allocate( VoidVolumeRatio                      (1:n_void_volumes) )
if(.not. allocated(CldPermHoopStrn      ) ) allocate( CldPermHoopStrn                      (1:naxialnodes) )
if(.not. allocated(ECR                  ) ) allocate( ECR                      (1:naxialnodes) )
if(.not. allocated(OxUptakeID1          ) ) allocate( OxUptakeID1                      (1:naxialnodes) )
if(.not. allocated(OxUpTakeID2          ) ) allocate( OxUpTakeID2                      (1:naxialnodes) )
if(.not. allocated(SEDPNNL              ) ) allocate( SEDPNNL                      (1:naxialnodes) )
if(.not. allocated(EffStrainPNNL        ) ) allocate( EffStrainPNNL                      (1:naxialnodes) )
if(.not. allocated(coefk                ) ) allocate( coefk                      (1:naxialnodes) )
if(.not. allocated(coefn                ) ) allocate( coefn                      (1:naxialnodes) )
if(.not. allocated(coefm                ) ) allocate( coefm                      (1:naxialnodes) )
if(.not. allocated(Emodulus             ) ) allocate( Emodulus                      (1:naxialnodes) )
if(.not. allocated(strainrateterm       ) ) allocate( strainrateterm                      (1:naxialnodes) )
if(.not. allocated(SEDEPRI              ) ) allocate( SEDEPRI                      (1:naxialnodes) )
if(.not. allocated(EffFastFluStrnHardExp) ) allocate( EffFastFluStrnHardExp                      (1:naxialnodes) )
if(.not. allocated(BetaThickness        ) ) allocate( BetaThickness                      (1:naxialnodes) )
if(.not. allocated(EffColdWkStrnHardExp ) ) allocate( EffColdWkStrnHardExp                      (1:naxialnodes) )
if(.not. allocated(OxygenConcenAve      ) ) allocate( OxygenConcenAve                      (1:naxialnodes) )
if(.not. allocated(EffColdWkStrenCoef   ) ) allocate( EffColdWkStrenCoef                      (1:naxialnodes) )
if(.not. allocated(OxygenUptake         ) ) allocate( OxygenUptake                      (1:naxialnodes) )
if(.not. allocated(OxStAlphaThkRemain   ) ) allocate( OxStAlphaThkRemain                      (1:naxialnodes) )
if(.not. allocated(OxStAlphaThkNearFuel ) ) allocate( OxStAlphaThkNearFuel                      (1:naxialnodes) )
if(.not. allocated(EffFastFluStrenCoef  ) ) allocate( EffFastFluStrenCoef                      (1:naxialnodes) )
if(.not. allocated(OxiPowerGen          ) ) allocate( OxiPowerGen                      (1:naxialnodes) )
if(.not. allocated(PrcntSatBeta         ) ) allocate( PrcntSatBeta                      (1:naxialnodes) )
if(.not. allocated(OxConcenAAO          ) ) allocate( OxConcenAAO                      (1:naxialnodes) )
if(.not. allocated(OxConcenABO          ) ) allocate( OxConcenABO                      (1:naxialnodes) )
if(.not. allocated(OxConcenACO          ) ) allocate( OxConcenACO                      (1:naxialnodes) )
if(.not. allocated(OxConcenADO          ) ) allocate( OxConcenADO                      (1:naxialnodes) )
if(.not. allocated(OxConcenAEO          ) ) allocate( OxConcenAEO                      (1:naxialnodes) )
if(.not. allocated(OxConcenAFO          ) ) allocate( OxConcenAFO                      (1:naxialnodes) )
if(.not. allocated(OxConcenAGO          ) ) allocate( OxConcenAGO                      (1:naxialnodes) )
if(.not. allocated(OxConcenAHO          ) ) allocate( OxConcenAHO                      (1:naxialnodes) )
if(.not. allocated(OxConcenAIO          ) ) allocate( OxConcenAIO                      (1:naxialnodes) )
if(.not. allocated(OxConcenAAI          ) ) allocate( OxConcenAAI                      (1:naxialnodes) )
if(.not. allocated(OxConcenABI          ) ) allocate( OxConcenABI                      (1:naxialnodes) )
if(.not. allocated(OxConcenACI          ) ) allocate( OxConcenACI                      (1:naxialnodes) )
if(.not. allocated(OxConcenADI          ) ) allocate( OxConcenADI                      (1:naxialnodes) )
if(.not. allocated(OxConcenAEI          ) ) allocate( OxConcenAEI                      (1:naxialnodes) )
if(.not. allocated(OxConcenAFI          ) ) allocate( OxConcenAFI                      (1:naxialnodes) )
if(.not. allocated(OxConcenAGI          ) ) allocate( OxConcenAGI                      (1:naxialnodes) )
if(.not. allocated(OxConcenAHI          ) ) allocate( OxConcenAHI                      (1:naxialnodes) )
if(.not. allocated(OxConcenAII          ) ) allocate( OxConcenAII                      (1:naxialnodes) )
if(.not. allocated(CrackVolume          ) ) allocate( CrackVolume                      (1:naxialnodes) )
if(.not. allocated(OpenPorTemp          ) ) allocate( OpenPorTemp                      (1:naxialnodes) )
if(.not. allocated(AveDishTemp          ) ) allocate( AveDishTemp                      (1:naxialnodes) )
if(.not. allocated(CentVoidVol          ) ) allocate( CentVoidVol                      (1:naxialnodes) )
if(.not. allocated(ExtentOfBow          ) ) allocate( ExtentOfBow                      (1:naxialnodes) )
if(.not. allocated(AxialNodLen          ) ) allocate( AxialNodLen                      (1:naxialnodes) )
if(.not. allocated(TerfacePres          ) ) allocate( TerfacePres                      (1:naxialnodes) )
if(.not. allocated(CrackWidth           ) ) allocate( CrackWidth                      (1:naxialnodes) )
if(.not. allocated(EinstabilStrain      ) ) allocate( EinstabilStrain                      (1:naxialnodes) )
if(.not. allocated(AxBurnup             ) ) allocate( AxBurnup                      (1:naxialnodes) )
if(.not. allocated(BOSOxideThick        ) ) allocate( BOSOxideThick                      (1:naxialnodes) )
if(.not. allocated(EOSOxideThick        ) ) allocate( EOSOxideThick                      (1:naxialnodes) )
if(.not. allocated(OpenPorVol           ) ) allocate( OpenPorVol                      (1:naxialnodes) )
if(.not. allocated(OpenPorosity         ) ) allocate( OpenPorosity                      (1:naxialnodes) )
if(.not. allocated(CladEffStress        ) ) allocate( CladEffStress                      (1:naxialnodes) )
if(.not. allocated(BulkCoolTemp         ) ) allocate( BulkCoolTemp                      (1:naxialnodes) )
if(.not. allocated(CladYieldStress      ) ) allocate( CladYieldStress                      (1:naxialnodes) )
if(.not. allocated(StressAtInstStrain   ) ) allocate( StressAtInstStrain                      (1:naxialnodes) )
if(.not. allocated(techf                ) ) allocate( techf                      (1:naxialnodes) )
if(.not. allocated(CesiumContent        ) ) allocate( CesiumContent                      (1:naxialnodes) )
if(.not. allocated(HydrogenContent      ) ) allocate( HydrogenContent                      (1:naxialnodes) )
if(.not. allocated(tschf                ) ) allocate( tschf                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceGAPI        ) ) allocate( WorkSpaceGAPI                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceEP1         ) ) allocate( WorkSpaceEP1                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceURC         ) ) allocate( WorkSpaceURC                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceTCMx        ) ) allocate( WorkSpaceTCMx                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceGAP         ) ) allocate( WorkSpaceGAP                      (1:naxialnodes) )
if(.not. allocated(nodchf               ) ) allocate( nodchf                      (1:naxialnodes+1) )
if(.not. allocated(BOSTemp              ) ) allocate( BOSTemp                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(EOSTemp              ) ) allocate( EOSTemp                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(EOSRad               ) ) allocate( EOSRad                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(BOSRad               ) ) allocate( BOSRad                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(EnrgyMeltP1          ) ) allocate( EnrgyMeltP1                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(EnrgyMeltZ           ) ) allocate( EnrgyMeltZ                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(EnrgyMelt            ) ) allocate( EnrgyMelt                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(EnrgyMeltZp1         ) ) allocate( EnrgyMeltZp1                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(RadialBoundO         ) ) allocate( RadialBoundO                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(DeformedRadiusOfMesh ) ) allocate( DeformedRadiusOfMesh                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(WorkSpaceEPP1        ) ) allocate( WorkSpaceEPP1                      (1:naxialnodes,1:3) )
if(.not. allocated(WorkSpacePINT        ) ) allocate( WorkSpacePINT                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceRCI         ) ) allocate( WorkSpaceRCI                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceRCO         ) ) allocate( WorkSpaceRCO                      (1:naxialnodes) )
if(.not. allocated(WorkSpaceReloc       ) ) allocate( WorkSpaceReloc                      (1:naxialnodes) )
if(.not. allocated(AxNodElevat          ) ) allocate( AxNodElevat                      (1:naxialnodes) )
if(.not. allocated(ureloc               ) ) allocate( ureloc                      (1:naxialnodes) )
if(.not. allocated(a1                   ) ) allocate( a1                      (1:10000) )
if(.not. allocated(CldElStrn            ) ) allocate( CldElStrn                      (1:naxialnodes,1:3) )
if(.not. allocated(CldPlasStrn          ) ) allocate( CldPlasStrn                      (1:naxialnodes,1:3) )
if(.not. allocated(CldPlasStrn0         ) ) allocate( CldPlasStrn0                      (1:naxialnodes,1:3) )
if(.not. allocated(CldStrn              ) ) allocate( CldStrn                      (1:naxialnodes,1:3) )
if(.not. allocated(CldStrnRat           ) ) allocate( CldStrnRat                      (1:naxialnodes,1:3) )
if(.not. allocated(CldStrnRat0          ) ) allocate( CldStrnRat0                      (1:naxialnodes,1:3) )
if(.not. allocated(HydrostatPress       ) ) allocate( HydrostatPress                      (1:naxialnodes) )
if(.not. allocated(FuelResidStrn        ) ) allocate( FuelResidStrn                      (1:naxialnodes,1:3) )
if(.not. allocated(FuelResidStrn0       ) ) allocate( FuelResidStrn0                      (1:naxialnodes,1:3) )
if(.not. allocated(Baln2Twall           ) ) allocate( Baln2Twall                      (1:16,1:16) )
if(.not. allocated(CldresidStrn         ) ) allocate( CldresidStrn                      (1:naxialnodes,1:3) )
if(.not. allocated(CldResidStrn0        ) ) allocate( CldResidStrn0                      (1:naxialnodes,1:3) )
if(.not. allocated(IodineContent        ) ) allocate( IodineContent                      (1:naxialnodes) )
if(.not. allocated(PelSrfStrn           ) ) allocate( PelSrfStrn                      (1:naxialnodes,1:3) )
if(.not. allocated(CldStress            ) ) allocate( CldStress                      (1:naxialnodes,1:3) )
if(.not. allocated(CldThermStrn         ) ) allocate( CldThermStrn                      (1:naxialnodes,1:3) )
if(.not. allocated(GRsv                 ) ) allocate( GRsv                      (4,1:nradialnodes,1:nradialnodes) )
if(.not. allocated(IndexPCMI            ) ) allocate( IndexPCMI                      (1:naxialnodes) )
if(.not. allocated(IndexPCMIOnce        ) ) allocate( IndexPCMIOnce                      (1:naxialnodes) )
if(.not. allocated(NumAzmuthNod         ) ) allocate( NumAzmuthNod                      (1:naxialnodes) )
if(.not. allocated(Ichf                 ) ) allocate( Ichf                      (1:naxialnodes) )
if(.not. allocated(Ih                   ) ) allocate( Ih                      (1:naxialnodes) )
if(.not. allocated(Ihtreg               ) ) allocate( Ihtreg                      (1:naxialnodes) )
if(.not. allocated(GapIndex             ) ) allocate( GapIndex                      (1:naxialnodes) )
if(.not. allocated(BOSGapIndex          ) ) allocate( BOSGapIndex                      (1:naxialnodes) )
if(.not. allocated(BOSGapIndex0         ) ) allocate( BOSGapIndex0                      (1:naxialnodes) )
if(.not. allocated(RuptFailIndex        ) ) allocate( RuptFailIndex                      (1:naxialnodes) )
if(.not. allocated(CladCollapseIndex    ) ) allocate( CladCollapseIndex                      (1:naxialnodes) )
if(.not. allocated(CladCollIndx0        ) ) allocate( CladCollIndx0                      (1:naxialnodes) )
if(.not. allocated(OldGapIndex          ) ) allocate( OldGapIndex                      (1:naxialnodes) )
if(.not. allocated(NodeSinterTemp       ) ) allocate( NodeSinterTemp                      (1:naxialnodes) )
if(.not. allocated(RodFailIndex         ) ) allocate( RodFailIndex                      (1:naxialnodes) )
if(.not. allocated(Ifstor               ) ) allocate( Ifstor                      (1:30) )
if(.not. allocated(ihData               ) ) allocate( ihData                      (1:30) )
if(.not. allocated(icglob               ) ) allocate( icglob                      (1:12,1:8) )
if(.not. allocated(acolct               ) ) allocate( acolct                      (1:665) )
if(.not. allocated(gasphs               ) ) allocate( gasphs                      (1:2*ntimesteps) )
if(.not. allocated(gasths               ) ) allocate( gasths                      (1:2*ntimesteps,1:2) )
if(.not. allocated(gbse                 ) ) allocate( gbse                      (1:5) )
if(.not. allocated(pelprm               ) ) allocate( pelprm                      (1:40) )
if(.not. allocated(pinput               ) ) allocate( pinput                      (1:60) )
if(.not. allocated(potput               ) ) allocate( potput                      (1:60) )
if(.not. allocated(tpln                 ) ) allocate( tpln                      (1:6,1:2) )
if(.not. allocated(tplbt1               ) ) allocate( tplbt1                      (1:6,1:2) )
if(.not. allocated(aexcb                ) ) allocate( aexcb                      (1) )
if(.not. allocated(dtpoa                ) ) allocate( dtpoa                      (1:pre_nt+2) )
if(.not. allocated(tplbot               ) ) allocate( tplbot                      (1:6,1:2) )
if(.not. allocated(Prop                 ) ) allocate( Prop                      (1:26) )
if(.not. allocated(nhprs                ) ) allocate( nhprs                      (1:naxialnodes) )
if(.not. allocated(ntprs                ) ) allocate( ntprs                      (1:naxialnodes) )
if(.not. allocated(nvprs                ) ) allocate( nvprs                      (1:naxialnodes) )
if(.not. allocated(npprs                ) ) allocate( npprs                      (1:naxialnodes) )
if(.not. allocated(acoold               ) ) allocate( acoold                      (1) )
if(.not. allocated(vfrad1               ) ) allocate( vfrad1                      (1:5) )
if(.not. allocated(vfrad2               ) ) allocate( vfrad2                      (1:5) )
if(.not. allocated(vfrad3               ) ) allocate( vfrad3                      (1:5) )
if(.not. allocated(elvrad               ) ) allocate( elvrad                      (1:5) )
if(.not. allocated(htclev               ) ) allocate( htclev                      (1:naxialnodes) )
if(.not. allocated(gbh                  ) ) allocate( gbh                      (1:2*ntimesteps) )
if(.not. allocated(hbh                  ) ) allocate( hbh                      (1:2*ntimesteps) )
if(.not. allocated(hinta                ) ) allocate( hinta                      (1:2*ntimesteps) )
if(.not. allocated(hupta                ) ) allocate( hupta                      (1:2*ntimesteps) )
if(.not. allocated(pbh                  ) ) allocate( pbh                      (1:2*ntimesteps) )
if(.not. allocated(tshrda               ) ) allocate( tshrda                      (1:ntimesteps) )
if(.not. allocated(htca                 ) ) allocate( htca                      (1:2*ntimesteps,1:naxialnodes) )
if(.not. allocated(tblka                ) ) allocate( tblka                      (1:2*ntimesteps,1:naxialnodes) )
if(.not. allocated(trad1                ) ) allocate( trad1                      (1:20,1:5) )
if(.not. allocated(trad2                ) ) allocate( trad2                      (1:20,1:5) )
if(.not. allocated(trad3                ) ) allocate( trad3                      (1:20,1:5) )
if(.not. allocated(flthit               ) ) allocate( flthit                      (1:40) )
if(.not. allocated(faxzq                ) ) allocate( faxzq                      (1:2*naxialnodes) )
if(.not. allocated(qaxzq                ) ) allocate( qaxzq                      (1:2*naxialnodes) )
if(.not. allocated(tempfc               ) ) allocate( tempfc                      (1:2*naxialnodes) )
if(.not. allocated(aflcht               ) ) allocate( aflcht                      (1:630) )
if(.not. allocated(prestm               ) ) allocate( prestm                      (1:2*ntimesteps) )
if(.not. allocated(hlqclp               ) ) allocate( hlqclp                      (1:2*ntimesteps) )
if(.not. allocated(temptm               ) ) allocate( temptm                      (1:2*ntimesteps) )
if(.not. allocated(fldrat               ) ) allocate( fldrat                      (1:2*ntimesteps) )
if(.not. allocated(tz2                  ) ) allocate( tz2                      (1:naxialnodes) )
if(.not. allocated(z1                   ) ) allocate( z1                      (1:naxialnodes) )
if(.not. allocated(z2                   ) ) allocate( z2                      (1:naxialnodes) )
if(.not. allocated(gz1                  ) ) allocate( gz1                      (1:naxialnodes) )
if(.not. allocated(gz2                  ) ) allocate( gz2                      (1:naxialnodes) )
if(.not. allocated(hz1                  ) ) allocate( hz1                      (1:naxialnodes) )
if(.not. allocated(hz2                  ) ) allocate( hz2                      (1:naxialnodes) )
if(.not. allocated(pz1                  ) ) allocate( pz1                      (1:naxialnodes) )
if(.not. allocated(pz2                  ) ) allocate( pz2                      (1:naxialnodes) )
if(.not. allocated(tz1                  ) ) allocate( tz1                      (1:Total) )
if(.not. allocated(aasth                ) ) allocate( aasth                      (1:16140) )
if(.not. allocated(eppinp               ) ) allocate( eppinp                      (1:2*naxialnodes) )
if(.not. allocated(radpel               ) ) allocate( radpel                      (1:2*naxialnodes) )
if(.not. allocated(dtplta               ) ) allocate( dtplta                      (1:pre_nt+2) )
if(.not. allocated(ngastmp              ) ) allocate( ngastmp                      (1:2) )
if(.not. allocated(modfal               ) ) allocate( modfal                      (1) )
if(.not. allocated(nswpm                ) ) allocate( nswpm                      (1) )
if(.not. allocated(irest2               ) ) allocate( irest2                      (1) )
if(.not. allocated(ncs                  ) ) allocate( ncs                      (1) )
if(.not. allocated(irest3               ) ) allocate( irest3                      (1) )
if(.not. allocated(indxjk               ) ) allocate( indxjk                      (1:nradialnodes) )
if(.not. allocated(nrc                  ) ) allocate( nrc                      (1,1) )
if(.not. allocated(dtmaxa               ) ) allocate( dtmaxa                      (1:pre_nt+2) )
if(.not. allocated(arest1               ) ) allocate( arest1                      (1:136) )
if(.not. allocated(azpang               ) ) allocate( azpang                      (1:naxialnodes) )
if(.not. allocated(fluxz                ) ) allocate( fluxz                      (1:2*naxialnodes) )
if(.not. allocated(tplna                ) ) allocate( tplna                      (1:6,1:2,1) )
if(.not. allocated(imaterials           ) ) allocate( imaterials                      (1:nradialnodes) )
if(.not. allocated(imatflag             ) ) allocate( imatflag                      (1:nradialnodes) )
if(.not. allocated(imatflag1            ) ) allocate( imatflag1                      (1:nradialnodes) )
if(.not. allocated(iDatapairs           ) ) allocate( iDatapairs                      (1:nradialnodes) )
if(.not. allocated(iheattablen          ) ) allocate( iheattablen                      (1:nradialnodes) )
if(.not. allocated(nm                   ) ) allocate( nm                      (1:3) )
if(.not. allocated(htco                 ) ) allocate( htco                      (1:naxialnodes) )
if(.not. allocated(tem                  ) ) allocate( tem                      (1:naxialnodes) )
if(.not. allocated(pbh1                 ) ) allocate( pbh1                      (1:2*ntimesteps) )
if(.not. allocated(pbh2                 ) ) allocate( pbh2                      (1:2*ntimesteps) )
if(.not. allocated(hlqcl                ) ) allocate( hlqcl                      (1:2*ntimesteps) )
if(.not. allocated(ts                   ) ) allocate( ts                      (1:2*ntimesteps) )
if(.not. allocated(gadoln               ) ) allocate( gadoln                      (1:naxialnodes) )
if(.not. allocated(oldepeff             ) ) allocate( oldepeff                      (1:naxialnodes) )
if(.not. allocated(oldeps               ) ) allocate( oldeps                      (1:naxialnodes) )
if(.not. allocated(afrap                ) ) allocate( afrap                      (1:40000) )
if(.not. allocated(amatpc               ) ) allocate( amatpc                      (1) )
if(.not. allocated(ispace               ) ) allocate( ispace                      (1:11) )
if(.not. allocated(emflag               ) ) allocate( emflag                      (1:21) )
if(.not. allocated(iok                  ) ) allocate( iok                      (1:5,1:naxialnodes) )
if(.not. allocated(iokold               ) ) allocate( iokold                      (1:5,1:naxialnodes) )
if(.not. allocated(FrapconTemp          ) ) allocate( FrapconTemp                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(qcold                ) ) allocate( qcold                      (1:20,1:20) )
if(.not. allocated(SEDPNNLold           ) ) allocate( SEDPNNLold                      (1:naxialnodes) )
if(.not. allocated(oldEffStrainPNNL     ) ) allocate( oldEffStrainPNNL                      (1:naxialnodes) )
if(.not. allocated(SEDEPRIold           ) ) allocate( SEDEPRIold                      (1:naxialnodes) )
if(.not. allocated(EPStrain1old         ) ) allocate( EPStrain1old                      (1:naxialnodes) )
if(.not. allocated(EPStrain2old         ) ) allocate( EPStrain2old                      (1:naxialnodes) )
if(.not. allocated(EPStrain3old         ) ) allocate( EPStrain3old                      (1:naxialnodes) )
if(.not. allocated(StressHoopOld        ) ) allocate( StressHoopOld                      (1:naxialnodes) )
if(.not. allocated(StressRadialOld      ) ) allocate( StressRadialOld                      (1:naxialnodes) )
if(.not. allocated(StressAxialOld       ) ) allocate( StressAxialOld                      (1:naxialnodes) )
if(.not. allocated(CladEffStressOld     ) ) allocate( CladEffStressOld                      (1:naxialnodes) )
if(.not. allocated(hsolold              ) ) allocate( hsolold                      (1:naxialnodes) )
if(.not. allocated(OldCldPlasStrn       ) ) allocate( OldCldPlasStrn                      (1:naxialnodes) )
if(.not. allocated(Iffrp                ) ) allocate( Iffrp                      (1:axial) )
if(.not. allocated(pclrlp               ) ) allocate( pclrlp                      (1:axial) )
if(.not. allocated(rmrlp                ) ) allocate( rmrlp                      (1:radial) )
if(.not. allocated(hgpfrp               ) ) allocate( hgpfrp                      (1:axial) )
if(.not. allocated(ElevatThermHydr      ) ) allocate( ElevatThermHydr                      (1:axial) )
if(.not. allocated(tmprlp               ) ) allocate( tmprlp                      (1:radial,1:axial) )
if(.not. allocated(bufrp                ) ) allocate( bufrp                      (1:axial) )
if(.not. allocated(pgpfrp               ) ) allocate( pgpfrp                      (1:axial) )
if(.not. allocated(vrlfrp               ) ) allocate( vrlfrp                      (1:axial) )
if(.not. allocated(drdfrp               ) ) allocate( drdfrp                      (1:axial) )
if(.not. allocated(ElevatFrap           ) ) allocate( ElevatFrap                      (1:axial) )
if(.not. allocated(HTC_L                ) ) allocate( HTC_L                      (1:axial) )
if(.not. allocated(HTC_V                ) ) allocate( HTC_V                      (1:axial) )
if(.not. allocated(TBulk_L              ) ) allocate( TBulk_L                      (1:axial) )
if(.not. allocated(TBulk_V              ) ) allocate( TBulk_V                      (1:axial) )
if(.not. allocated(gasfraction          ) ) allocate( gasfraction                      (1:ngases) )
if(.not. allocated(gsmol0               ) ) allocate( gsmol0                      (1:ngases) )
if(.not. allocated(dtdkta               ) ) allocate( dtdkta                      (1:pre_nt+2) )
if(.not. allocated(dktouts              ) ) allocate( dktouts                      (1,1) )
if(.not. allocated(buradv               ) ) allocate( buradv                      (1:naxialnodes,1:nradialnodes) )
if(.not. allocated(bufrad               ) ) allocate( bufrad                      (1:nradialnodes,1:naxialnodes) )
if(.not. allocated(axlinpower           ) ) allocate( axlinpower                  (1:naxialnodes) )
